---
title: Strider
description: A strict formatter and unified code checker for Go, in one static binary.
template: splash
head:
  - tag: style
    content: 'body { overflow-x: hidden; }'
hero:
  title: Strider
  tagline: The ranger for your Go code. One fast, self-contained binary that formats source and hunts down trouble — before it reaches review.
  image:
    file: ../../assets/strider.png
    alt: Strider, the gopher ranger
  actions:
    - text: Get started
      link: /getting-started/
      icon: right-arrow
      variant: primary
    - text: Browse every check
      link: /checks/
      variant: minimal
---

import Terminal from '../../components/Terminal.astro';
import catalog from '../../generated/catalog.json';
import sftpgoBenchmark from '../../generated/sftpgo-benchmark.json';

export const sftpgoFormat = sftpgoBenchmark.format_ms.toLocaleString('en-US');
export const sftpgoCheck = sftpgoBenchmark.check_ms.toLocaleString('en-US');
export const benchmarkProject = sftpgoBenchmark.project === 'sftpgo'
  ? 'SFTPGo'
  : sftpgoBenchmark.project;

export const checkDemo = `<span class="t-dim">$</span> <span class="t-cmd">strider check ./...</span>
<span class="t-warn">no-defer-in-loop</span>: <strong>avoid defer inside loops</strong>
   <span class="t-path">┌─ internal/server/pool.go:88:9</span>
   <span class="t-path">│</span>
87 <span class="t-path">│</span>     for rows.Next() {
88 <span class="t-path">│</span>         <span class="t-warn">defer rows.Close()</span>
89 <span class="t-path">│</span>     }

<span class="t-err">unclosed-http-response-body</span>: <strong>response body is not closed</strong>
   <span class="t-path">┌─ internal/server/pool.go:41:2</span>
   <span class="t-path">│</span>
40 <span class="t-path">│</span>  }
41 <span class="t-path">│</span>  <span class="t-err">conn.Close()</span>
42 <span class="t-path">│</span>  return nil

<span class="t-warn">no-defer-in-loop</span>          <span class="t-warn">1</span>
<span class="t-err">unclosed-http-response-body</span>  <span class="t-err">1</span>
<strong>2 issues:</strong> <span class="t-err">1 error</span><strong>,</strong> <span class="t-warn">1 warning</span>`;

<div class="land">

<div class="land-stats">
  <div class="land-stat"><strong>1</strong><span>static binary, zero deps</span></div>
  <div class="land-stat"><strong>{catalog.checks}</strong><span>checks</span></div>
  <div class="land-stat"><strong><em>{sftpgoFormat}</em> ms</strong><span>to format {benchmarkProject}</span></div>
  <div class="land-stat"><strong><em>{sftpgoCheck}</em> ms</strong><span>to check {benchmarkProject}</span></div>
</div>

<h2 class="land-heading"><span class="land-kicker">One command</span>Diagnostics with the code that caused them</h2>

`strider check` folds formatting drift, style policy, and package-aware
correctness analysis into a single diagnostic pass. It is read-only by default,
with explicit safe and unsafe fix modes when source should be updated. No plugin
zoo, no tool matrix — files are parsed once and shared across every selected
check.

<Terminal html={checkDemo} />

</div>
