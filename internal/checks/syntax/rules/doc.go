// Package rules contains Strider's concrete-syntax check catalog. The
// package owns a single lossless CST traversal per file; focused check-family
// modules contain the checks and their nearby helpers.
package rules
