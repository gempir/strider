// Package rules contains Strider's concrete-syntax lint rule catalog. The
// package owns a single lossless CST traversal per file; focused rule-family
// modules contain the checks and their nearby helpers.
package rules
