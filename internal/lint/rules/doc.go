// Package rules contains Strider's extended native lint rule catalog and AST
// checks. The package owns a single traversal per file; focused rule-family
// modules contain the checks and their nearby helpers.
package rules
